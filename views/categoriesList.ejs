<!DOCTYPE html>
<html lang="en">
	<head>
		<% include head %>
		<title><%= title %></title>
	</head>
	<body>
		<header>
			<% include navBar %>
			<h2><%= title %></h2>
		</header>
		<main>
			<div class="category-container">
				<% categories.forEach(category => {%>
				<a href="<%= category.url%>" class="category-card">
					<%= category.name%>
				</a>
				<% }) %>
			</div>
		</main>
	</body>
</html>
