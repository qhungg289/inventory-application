<!DOCTYPE html>
<html lang="en">
	<head>
		<% include head %>
		<title><%= title %></title>
	</head>
	<body>
		<header>
			<% include navBar %>
			<h1><%= title %></h1>
		</header>
		<main>
			<% if (items.length === 0) { %>
			<p>Are you really want to delete this category!</p>
			<form action="" method="post">
				<input type="hidden" name="id" value="<%= category._id %>" />
				<input type="submit" value="DELETE" class="button warning" />
			</form>
			<% } else { %>
			<p>
				There are items in this category right now. Consider delete all the
				items first before delete the category.
			</p>
			<ul>
				<% items.forEach(item => { %>
				<li>
					<a href="<%= item.url %>"><%= item.name %></a>
				</li>
				<% }) %>
			</ul>
			<a href="<%= category.url %>" class="button">GO BACK</a>
			<% } %>
		</main>
	</body>
</html>
